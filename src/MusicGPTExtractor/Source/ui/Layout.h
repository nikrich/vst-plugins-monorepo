#pragma once

namespace Layout
{
// Window dimensions (600x700)
inline constexpr int kWindowWidth  = 600;
inline constexpr int kWindowHeight = 700;

// Spacing
inline constexpr int kPadding = 16;
inline constexpr int kGap = 12;

// Section heights
inline constexpr int kHeaderHeight = 80;
inline constexpr int kDropZoneHeight = 150;
inline constexpr int kStatusHeight = 30;
inline constexpr int kTransportHeight = 40;
inline constexpr int kMixerHeight = 300;

// Slider dimensions
inline constexpr int kSliderWidth = 80;
inline constexpr int kSliderHeight = 200;
}
